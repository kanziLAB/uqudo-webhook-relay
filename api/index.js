'use strict';
// Vercel serverless entry point. server.js exports the Express app when
// process.env.VERCEL is set; every route is served through this one function.
module.exports = require('../server');
